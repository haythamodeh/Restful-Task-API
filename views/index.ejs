<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>

    <title>Basic Mongoose</title>
    <style>
        .box-wrapper {
            width: 400px;
            margin: 0 auto;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                    <div class="box-wrapper">
                            <form action="/quotes" method="POST">
                                <h3 class="text-center">Welcome to Quoting Dojo</h3>
                                <% if(messages.registration) { %>
                                    <% for (var x of messages.registration) { %>
                                        <h5 class="text-danger"><%= x %></h5>
                                    <% } %>
                                <% } %>
                                <div class="form-group">
                                    <label for="name">Name</label>
                                    <input type="text" name="name" class="form-control">
                                </div>
                                <div class="form-group">
                                    <label for="age">Your Quote</label>
                                    <input type="text" name="quote" class="form-control">
                                </div>
                                <input type="submit" value="Add my Quote!" class="btn btn-success">
                                <a href="/quotes" class="btn btn-primary">Skip to Quotes!</a>
                            </form>
                    </div>
            </div>
        </div>
    </div>
</body>
</html>